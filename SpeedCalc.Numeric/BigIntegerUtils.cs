using System;
using System.Globalization;

namespace SpeedCalc.Numeric
{
    static class BigIntegerUtils
    {
        public static string ToString(in BigInteger value, string format, NumberFormatInfo formatInfo)
        {
            throw new NotImplementedException();
        }

        public static bool TryParse(string value, NumberStyles style, NumberFormatInfo formatInfo, out BigInteger result)
        {
            throw new NotImplementedException();
        }
    }
}
